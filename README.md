# simple_portscanner
simple sequential TCP port scanner in python  built as a learning project (sockets, networking fundamentals)


## Current status: v0.1.0

working sequential scanner, tests a rnge of TCP ports using `socket.connect_ex()`.

## Roadmap
- [x] v0.1 — basic sequential scanner
